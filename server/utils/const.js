const PAGE_SIZE = 20;
const PAGE_SIZE_COMMENT = 5;

export default PAGE_SIZE;

// const { API_URL, MAX_THREADS } = require("./constants");

// export function define(name, value) {
//   Object.defineProperty(Array.prototype, name, {
//     value: value,
//     enumerable: true,
//   });
// }

// define("PAGE_SIZE", 20);
// define("PAGE_SIZE_COMMENT", 5);
